JWT Authentication API

A practice project that implements stateless authentication with JSON Web Tokens (JWT). Users can register, log in, and access protected routes using a signed token.

Built for learning. It covers the core auth flow: password hashing, token signing, token verification, and route protection.
